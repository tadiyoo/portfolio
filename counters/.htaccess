# Block all direct web access to the raw counter data files.
# Only counter.php (server-side) ever reads/writes them.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
